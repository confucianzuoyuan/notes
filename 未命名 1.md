$$
q(z_{t-1}|z_{t}) = \frac{q(z_{t}|z_{t-1})q(z_{t-1})}{q(z_{t})}
$$

为什么可以写成

$$
q(z_{t-1}) = \int q(z_{t-1}|x)p(x)\mathrm{d}x
$$

边缘化

若存在联合分布 $q(z_{t-1},x)$ ，则有 $q(z_{t-1})=\int q(z_{t-1},x)\mathrm{d}x$ 。

又因为 $q(z_{t-1},x)=q(z_{t-1}\mid x)p(x)$ 。所以成立。

意思是：

- 理论上我们想得到的分布是 $q(z_{t-1}) = \int q(z_{t-1}\mid x)\,p(x)\,\mathrm{d}x$。这里需要对未知的数据密度 $p(x)$ 做积分，这通常不可直接计算，因为真实的数据分布 $p(x)$ 我们并不知道其解析形式。

- 实务中常用经验分布近似 $p(x)$：把积分换成对训练样本的平均。若训练集为 $\{x_i\}_{i=1}^N$，则 $q(z_{t-1}) \approx \frac{1}{N}\sum_{i=1}^N q(z_{t-1}\mid x_i)$ 。

- 如果条件分布 $q(z_{t-1}\mid x)$ 在给定 $x$ 时是一个高斯分布（很多模型中常见，例如变分推断里令 $q(z\mid x)=\mathcal N(\mu(x), \Sigma(x))$），那么上面的经验平均就是许多高斯分布的加权和（这里每个样本的权重大致为 $1/N$）。而“多个高斯分布的加权和”正是“高斯混合分布”（Gaussian Mixture, GMM）的形式：

$$
q(z_{t-1}) \approx \frac{1}{N}\sum_{i=1}^N \mathcal N\big(z_{t-1};\,\mu(x_i), \Sigma(x_i)\big)
$$

- 因此，结论是：由于真实的 $p(x)$ 不可积（未知），用训练样本近似积分会把目标边缘分布变成“许多条件高斯的平均”，也就是一个复杂的高斯混合分布。这个分布通常没有简单的闭式表达（除非各高斯共享特定参数），计算某些量（如精确熵、KL等）也会变得困难。

$$
q(z_{t}) = \sum_{z_{t-1}}q(z_{t}|z_{t-1})q(z_{t-1})
$$

$$
q(z_{t})=\int q(z_{t}|z_{t-1})q(z_{t-1})\mathrm{d}z_{t-1}
$$

贝叶斯公式

$$
q(z_{t-1}|z_{t}) = \frac{q(z_{t}|z_{t-1})q(z_{t-1})}{q(z_{t})}
$$

贝叶斯推断

$$
q(z_{t-1}|z_{t})\propto q(z_{t}|z_{t-1})q(z_{t-1})
$$

因为我们要计算的是，在 ==固定== $z_{t}$ 的情况下，求 $z_{t-1}$ 的概率，所以 $q(z_{t})$ 是个常数。可以忽略掉。

当 $q(z_{t}|z_{t-1})$ 的方差足够小的时候，乘上 $q(z_{t-1})$ 可以近似为为高斯分布，如果方差趋近于 0，则逼近于高斯分布。

![[马尔可夫逆向.excalidraw|1000]]

