$$
f(x) = 10(x_{2} - x_{1}^2)^2 + (1-x_{1})^2
$$
```python
import numpy as np
import matplotlib.pyplot as plt

# 1. 定义 Rosenbrock 函数
def f(x1, x2):
    return 10 * (x2 - x1**2)**2 + (1 - x1)**2

# 2. 准备数据范围
# 我们选择 x1 在 -2 到 2 之间，x2 在 -1 到 3 之间
x1 = np.linspace(-2, 2, 400)
x2 = np.linspace(-1, 3, 400)

# 3. 创建坐标网格
X1, X2 = np.meshgrid(x1, x2)
Z = f(X1, X2)

# 4. 绘图
plt.figure(figsize=(10, 7))

# 绘制等高线，levels 控制线条的密度，cmap 设置颜色映射
# 使用对数刻度（LogLocator）是因为该函数数值跨度很大，对数显示更清晰
contours = plt.contour(X1, X2, Z, levels=np.logspace(-1, 3, 20), cmap='viridis')

# 添加等高线数值标签
plt.clabel(contours, inline=True, fontsize=8)

# 标记全局最小值点 (1, 1)
plt.plot(1, 1, 'ro', label='Global Minimum (1, 1)')

# 设置图表细节
plt.title('Contour Plot of $f(x) = 10(x_2 - x_1^2)^2 + (1 - x_1)^2$', fontsize=14)
plt.xlabel('$x_1$', fontsize=12)
plt.ylabel('$x_2$', fontsize=12)
plt.colorbar(label='Function Value $f(x)$')
plt.legend()
plt.grid(alpha=0.3)

plt.show()
```


$$
f_{k} = 11, \nabla f_{k} = \begin{bmatrix}
-2 \\ 20
\end{bmatrix}, B_{k} = \nabla^2f_{k}=\begin{bmatrix}
-38 & 0 \\ 0 & 20
\end{bmatrix}
$$

公式为

$$
f_{k} + p^T \nabla f_{k} + \frac{1}{2}p^TB_{k}p - 1 = 0
$$

可视化出等高线

$$
f_{k} + p^T \nabla f_{k} + \frac{1}{2}p^TB_{k}p - 12 = 0
$$

可视化出等高线





